# Primuse 手机端完整重设计主 Prompt

> 使用方法：把本文件从“Prompt 正文开始”到“Prompt 正文结束”完整复制给另一个 AI。若对方能够读取当前工程，让它先审计代码再设计/实现；若对方只能做视觉设计，则要求它按相同功能清单输出完整设计系统、页面和交互原型。

---

## Prompt 正文开始

你是一名资深移动产品设计总监、音乐类 App 交互设计师和 SwiftUI 架构师。现在需要对一个已有的原生 Apple 平台音乐 App **Primuse（猿音）**做一次以 iPhone 为主的完整 UI/UX 重设计，并让用户可以在 App 内实时切换多套完整视觉风格。

这不是“换一个强调色”“做几张概念图”或“只改首页”的任务。你必须在**不丢失现有功能、不破坏播放状态和信息架构**的前提下，建立可落地的语义化主题系统，让导航、页面布局、卡片、列表、播放器、歌词、弹窗、图表、加载态、空状态、错误态、批量选择态等全部随风格同步变化。另有一个独立需求：新增适合长时间摆放展示的“全屏展示模式”，可防止自动息屏，并包含多种可切换的酷炫音频视觉效果。

如果你能访问源码：请先完整阅读现有 SwiftUI 工程、资源、本地化和服务层，以代码事实校验下面的功能基线；不要凭想象删减功能。如果你不能访问源码：以下功能基线视为权威需求，必须全部保留。

### 一、产品定位与基本原则

Primuse 是一个多音乐源、高音质、重度资料库管理型播放器，兼顾本地音乐、NAS、网络协议、媒体服务器、云盘、Apple Music 和网络电台。它不是仅面向流媒体歌单浏览的轻量播放器。

设计目标：

1. 对普通用户足够直观：打开即可听、搜索、收藏、看歌词。
2. 对进阶用户保留专业能力：来源管理、扫描、离线缓存、元数据、格式信息、音效、智能歌单、统计等不能被视觉重设计掩盖。
3. 音乐内容始终是视觉主角，封面、歌词、播放状态优先于装饰。
4. 风格可以强烈，但交互语义必须稳定；切换风格不应让用户重新学习每个功能在哪里。
5. 对万级甚至数万首曲库保持流畅，避免在长列表中滥用实时模糊、粒子或复杂逐行动画。
6. iPhone 是本次主目标；同时给出紧凑屏、大屏 iPhone、横屏和 iPad 自适应策略，但不要把手机页面直接放大成平板页面。
7. 支持浅色、深色、动态字体、VoiceOver、减弱动态效果、增强对比度、RTL 和长文本本地化。

### 二、现有移动端全局信息架构

#### 1. 一级导航

iPhone 底部为 4 个一级 Tab：

- 首页
- 资料库
- 搜索
- 设置

任意主页面只要存在正在播放内容，底部 Tab 上方持续显示迷你播放器。点击迷你播放器进入全屏“正在播放”。播放页关闭后回到原页面和原滚动位置，播放和队列不中断。

iPad/宽屏可切换为侧边栏 + 详情结构；侧边栏除四个一级入口外，可以直达歌曲、专辑、艺术家、歌单和电台。窄屏恢复底部 Tab。

#### 2. 迷你播放器

- 展示当前封面、歌曲名、播放/暂停按钮和下一首按钮。
- 网络电台播放时，播放/暂停语义变为播放/停止，并允许切换上一/下一电台。
- 点击主体进入完整播放页。
- 在迷你播放器主体左右滑动可切换上一首/下一首，并提供方向提示、轻微位移反馈和触觉反馈。
- 加载中显示明确进度；不可用操作必须禁用而不是点击后无反应。
- 必须兼容底部 Tab、安全区、键盘和批量操作栏，不能互相遮挡。

#### 3. 完整播放页的出现与关闭

- 从迷你播放器以连续、可感知的转场展开为全屏播放页。
- 支持从顶部区域向下拖拽关闭；支持从左侧边缘向右拖拽返回。
- 拖拽过程中页面圆角、缩放和下方迷你播放器应形成连续空间关系。
- 减弱动态效果开启时改用淡入淡出或近乎即时切换。

#### 4. 全局深链与状态恢复

- Spotlight/系统搜索可以直接播放歌曲或打开专辑、艺术家、歌单。
- Handoff 可恢复歌曲、队列、播放时间、播放/暂停、随机和循环状态。
- 应用重启、主题切换、横竖屏切换不能无故清空导航栈、搜索词、选择状态、当前播放和队列。
- 对不安全 HTTP、自签名证书、蜂窝网络补全等情况保留明确的风险确认和“仅本次/始终允许/仅 Wi‑Fi”等选择。

### 三、逐页面功能与交互基线

以下不是建议列表，而是重设计时必须保留的完整功能范围。

#### P00. 首次启动与引导

- 仅在用户尚未看过引导且没有任何音乐源时全屏出现。
- 4 页可横向翻页的介绍：多来源、元数据能力、跨设备/播放体验、个性化。
- 每页包含主视觉、标题、能力要点和简短说明；有页码指示。
- 主按钮为“下一步/完成”，前几页提供“关闭/跳过”。
- 完成后可自然进入“添加第一个音乐源”，不应让用户回到空首页再寻找入口。
- 引导视觉应跟随当前默认完整主题，但内容可独立使用更强的品牌叙事。

#### P01. 首页—音乐面

首页有“音乐”和“电台”两个互斥的正反面，可通过顶部切换控件或翻面动作切换，并有 3D 翻转/触觉反馈。切换当前媒体类型时需正确停止上一种媒体的播放，不能出现歌曲和电台同时出声。

音乐面包含：

1. 固定在顶部的 Hero：按时间显示问候语、资料库混合封面拼贴、“随机播放”和“全部播放”。
2. 继续聆听：两行横向列表，点击歌曲直接播放，并以该列表形成播放队列。
3. 快捷访问：最多 5 个用户固定的项目，可为“喜欢的歌曲”、专辑、艺术家或歌单；点击进入对应详情。
4. 为你推荐：本地计算的推荐歌曲，以卡片展示推荐理由；点击直接播放。
5. 歌单：紧凑列表，点击进入详情。
6. 常听艺术家：横向圆形头像列表，点击进入艺术家详情。
7. 最近添加专辑：手机两列网格；点击可快速播放该专辑，同时要让“进入详情”的意图清晰可达。
8. 聆听概览：本期播放次数/时长等简要数据，点击进入完整统计。

首页模块除 Hero 外均可在设置中显示/隐藏和拖拽排序。必须为以下状态设计完整界面：首次加载、刷新、部分模块无数据、整库为空、来源扫描中、元数据仍在后台补全、错误和离线。

#### P02. 首页—电台面

- 顶部焦点卡展示当前电台 Logo、名称、直播元数据；只有正在播放时显示 LIVE 状态。
- 提供上一台、播放/停止、下一台。
- 在焦点卡上横向滑动可循环切换电台。
- 下方为自适应电台墙；点击某台选中并播放，再次点击当前台执行合理的停止/继续逻辑。
- 当前电台必须有清晰但不只依赖颜色的选中标识。
- 提供“添加电台”和“管理电台”入口。
- HTTP 流地址必须出现安全风险确认。
- 没有电台时提供清晰空状态和添加入口。

#### P03. 资料库总览

- 顶部快捷访问横向区，展示最多 5 个固定项目。
- “编辑快捷访问”以 Sheet 打开：搜索所有可固定项目，点击选中/取消，最多 5 个，支持拖动排序，并显示数量约束。
- 分类入口：歌曲、专辑、艺术家、歌单、电台；每行显示总数和重叠封面预览。
- 曲库为空时提供“管理音乐源”和“管理电台”两个明确入口。
- 从系统深链进入资料库后可直接打开指定专辑、艺术家或歌单。

#### P04. 歌曲—文件夹浏览

- 用户可在“文件夹视图”和“平铺视图”之间切换，并可在设置中选择默认模式。
- 文件夹根层按音乐源、扫描目录和特殊分类组织；特殊分类包括 Apple Music 资料库、歌单内/未进歌单、未分类等。
- 文件夹支持展开/收起和逐层进入；详情页显示子目录及当前目录的直接歌曲。
- 提供当前目录“全部播放”。
- 显示加载、无权限、离线、目录为空、连接失败和重试状态。
- 切换文件夹/平铺模式时要有明确过渡和加载反馈，不能造成“歌曲消失”的错觉。

#### P05. 歌曲—平铺列表与单曲操作

- 面向 10,000+ 歌曲优化的虚拟长列表。
- 支持按标题、艺术家、专辑、加入日期、文件格式排序；大库排序时显示进度。
- 点击歌曲立即播放，并以当前列表生成从该歌曲开始的循环队列。
- 单行显示封面、标题、艺术家/专辑、时长、来源/格式等必要信息；可显示独立 MV、当前播放、加载、离线缓存、失败重试、元数据补全等状态。
- 行内“更多”或上下文菜单提供：刮削歌曲信息、标签编辑、歌词编辑、加入歌单、相似歌曲、离线下载/固定/重试/移除、重试元数据、歌曲信息、分享，以及在来源允许时删除源文件。
- 删除源文件必须二次确认，并明确它不同于“从资料库移除”。只读来源和 Apple Music 必须隐藏或禁用不适用操作。

#### P06. 歌曲批量选择

- 长按约 0.45 秒进入多选，并默认选中触发歌曲；也可从菜单主动进入。
- 多选态点击行切换选择；顶部显示已选数量、全选/取消全选、退出。
- 底部批量栏取代普通 Tab 区，核心操作为：加入歌单、加入队列、更多。
- “更多”包含：下一首播放/插入接下来、离线下载、只刮削缺失信息、从当前歌单移除、从资料库移除、在权限允许时删除源文件。
- 批量操作必须显示处理进度、成功/失败/跳过数量，并说明只读或不支持的项目为什么被跳过。
- 危险操作要确认；可恢复的删除要提供撤销或进入“最近删除”。

#### P07. 专辑列表与专辑详情

- 专辑列表：手机为自适应两列网格，显示封面、专辑名、艺术家；点击进入详情。
- 专辑详情：大封面、标题、艺术家、年份、歌曲数、总时长。
- 主操作：全部播放、随机播放、整张专辑离线下载。
- 曲目列表支持点击播放、单曲更多操作和长按批量选择。
- 艺术家名可跳转到艺术家详情；当前播放歌曲有明确状态。
- 设计专辑封面缺失、年份缺失、合辑、多碟、扫描中和空专辑状态。

#### P08. 艺术家列表与艺术家详情

- 艺术家列表使用圆形或主题化头像，显示专辑数和歌曲数。
- 详情页包含头像、名称、统计、全部播放、随机播放。
- 分为“专辑”网格和“全部歌曲”列表；专辑进入详情，歌曲可播放和多选。
- 未知艺术家、多个同名艺术家、头像缺失和仅有单曲无专辑时仍需良好呈现。

#### P09. 歌单列表

- 分普通歌单和智能歌单两组。
- 新建菜单包含“新建普通歌单”“新建智能歌单”“批量管理”。
- 普通歌单新建时输入名称；行内展示首曲封面、歌曲数、更新时间。
- 用户创建的普通歌单可侧滑删除；系统喜欢歌单受保护；镜像/服务器歌单为只读，可选择“在 Primuse 中隐藏”而不是删除源端数据。
- 批量管理支持多选、全选和删除，受保护项不可选并解释原因。

#### P10. 普通歌单详情

- 顶部封面、名称、歌曲数；主操作为全部播放、随机播放、整单离线下载。
- 歌曲列表支持播放、单曲操作和批量选择。
- 用户歌单可移除歌曲；镜像歌单保持只读。
- 更多操作：批量选择、拖拽调整顺序、加入播放队列、下一首播放、仅刮削缺失元数据、导出 M3U8/JSON、删除用户歌单或隐藏镜像歌单。
- 批量刮削要显示进度、可取消并在结束后给出结果提示。
- 重排模式有取消/完成，退出前不要丢失已确认顺序。

#### P11. 智能歌单详情与规则编辑器

- 智能歌单详情使用明显但不过度抢内容的智能/闪光识别，显示名称、命中数和规则摘要。
- 支持全部播放、随机、离线下载、查看匹配歌曲、编辑规则。
- 规则编辑器支持多个规则组；每组可设包含/排除，组内 AND/OR，组间 AND/OR。
- 规则字段包括：标题、艺术家、专辑、流派、年份、格式、加入日期、时长、文件大小、比特率、来源、播放次数、上次播放、是否位于某歌单。
- 运算符包括：等于、不等于、包含、不包含、大于、小于、区间等；输入控件根据字段类型变化。
- 支持排序字段、升降序、随机排序、可选数量上限。
- 支持添加/删除规则和规则组、保存、删除智能歌单及危险操作确认。
- 编辑过程中实时显示预计匹配数或小型预览，但避免大库每次输入都卡顿，必要时节流。

#### P12. 电台管理、编辑与批量导入

- 电台管理页为卡片网格；卡片可播放/停止，显示优先级、Logo、名称、流地址摘要和当前状态。
- 普通模式提供添加、批量添加、按名称排序、进入管理模式。
- 管理模式支持多选/全选、置顶、单条编辑、导出、删除；单条上下移动也可从上下文菜单完成。
- 新建/编辑电台包含名称、流地址、可选 Logo 图片、移除 Logo、测试播放、保存/取消。
- 不安全 HTTP 地址在测试和保存时都需明确确认。
- 批量导入有 3 个入口：粘贴文本/剪贴板、选择 M3U/M3U8/PLS 文件、搜索在线电台目录。
- 批量结果按“可播放、重复、无效”标记；可一键选择所有可播放项，也可逐项选择；显示重复于哪个已有电台，最后按选择数量批量加入。

#### P13. 搜索

- 独立搜索页，顶部系统化搜索框。
- 空查询时显示最近搜索，点击可复用，侧滑可删除，支持全部清除；同时显示本地资料库歌曲/专辑等数量概览。
- 输入后约 200ms 防抖；首次搜索显示骨架/加载，旧结果刷新时用轻量顶部进度，不要整页闪烁。
- 同时搜索：本地歌曲元数据、拼音/模糊匹配、全文歌词、专辑，以及已授权时的 Apple Music 目录。
- 结果按专辑、精确/普通歌曲、歌词命中、模糊命中、Apple Music 分组。
- 歌词结果显示时间戳和最多约 3 行上下文；点击后播放歌曲、打开完整播放页，并跳转到命中的歌词时刻/上下文。
- 本地结果可使用单曲更多操作和批量选择；Apple Music 结果遵循 MusicKit 能力，不显示本地文件专属操作。
- Apple Music 区域要分别设计：权限未决定、拒绝、加载、失败、无结果、正常结果。
- 无资料库、无结果、网络不可用和输入过短等状态需清晰区分。

#### P14. 标准“正在播放”页

普通歌曲播放页至少包含：

- 由封面提取颜色生成的动态氛围背景，同时确保文字对比度。
- 封面或 MV 画面、歌曲名、艺术家、专辑、音质/格式徽标。
- 点击艺术家和专辑进入对应详情。
- 喜欢按钮固定对应“喜欢的歌曲”歌单。
- 进度条可拖动，显示当前时间/总时长。
- 随机、上一首、播放/暂停/加载、下一首、循环关闭/列表/单曲。
- 系统音量、歌词/封面切换、AirPlay、播放队列。
- 底部或信息区可显示文件格式、采样率、来源等专业信息，但不能压过主控。
- 有 MV 边车资源时可切换音频/视频，并支持横屏全屏视频。

播放页有以下显示形态：

1. 标准封面模式。
2. 标准歌词模式：同步滚动，当前句清晰突出，支持逐字歌词时做字级进度。
3. 沉浸歌词模式：歌词占满屏幕，控制层约 4 秒自动隐藏；点击唤出；可锁定防误触；提供悬浮播放控制、退出、喜欢和更多。
4. iPad/横向宽屏双栏：左侧封面与控制，右侧常驻歌词。
5. MV 横屏/全屏模式。

“更多”菜单必须覆盖：加入歌单、刮削歌曲、相似歌曲、编辑标签、编辑歌词、歌曲信息、前往专辑、前往艺术家、在 Apple Music 打开、分享、投送到 DLNA/AirPlay 设备、歌词字号、歌词翻译开关、睡眠定时、播放速度、在允许时删除源文件。

睡眠定时支持 5/15/30/45/60 分钟、当前歌曲结束时停止，以及取消当前定时。播放速度快捷值为 0.5×、0.75×、1×、1.25×、1.5×、1.75×、2×。

Apple Music 模式复用同一播放页和整体体验，但隐藏或禁用刮削、标签、源文件删除、EQ/速度等无法落地的本地功能，并说明原因。

#### P15. 网络电台播放页

- 大 Logo、名称、实时节目/曲名元数据、LIVE 状态和直播累计时长。
- 上一台、停止/播放、下一台、系统音量、AirPlay。
- 可分享电台 URL，并显示流格式、比特率等技术信息。
- 直播没有确定总时长时禁止伪造普通歌曲进度条。
- 网络重连、缓冲、地址失效和 HTTP 风险要有独立状态。

#### P16. 播放队列

- 以中/大型 Sheet 打开，分“正在播放”“接下来”“已播放”三组。
- 点击任意条目立即从该队列位置播放。
- “接下来”支持拖拽重排；无障碍用户可用增减动作移动。
- 当前播放行显示波形/加载态，已播放区域弱化但仍可重新点击。
- 长队列使用惰性列表；空队列有解释性空状态。
- 随机和循环模式下，重复轮次中的同一首歌仍要有稳定的队列条目标识，避免拖错对象。

#### P17. 相似歌曲、歌曲信息与加入歌单

- 相似歌曲页以当前歌曲为种子，组合本地匹配和外部服务候选；支持“歌曲电台”和“相似混合”，点击结果播放。
- 区分加载、错误、无匹配、本地结果和在线候选。
- 歌曲信息页展示来源、路径、格式、编解码、位深、采样率、比特率、声道、时长、文件大小、缓存等可获得的技术信息。
- 加入歌单 Sheet 展示可编辑歌单及歌曲数；点击可加入/移除，并用勾选状态反馈。系统喜欢歌单和镜像只读歌单遵循各自规则。

#### P18. 标签编辑器

- 编辑封面、标题、艺术家、专辑、年份、流派、曲号、碟号和歌词。
- 展示原文件名，并可按常见“艺术家 - 标题/序号”规则一键拆分，应用前给出预览。
- 标题、艺术家和流派提供来自文件名、同目录、同专辑和现有曲库高频值的候选 Chips。
- 提供乱码/编码修复入口。
- 歌词区域显示是否有歌词、可写回来源文件还是只能保存在本地，并跳转完整歌词编辑器。
- 支持恢复原始值、保存进度、错误说明；来源只读时必须明确哪些变化只能保存在本地数据库。

#### P19. 歌词编辑器

- 至少有“文本编辑”“打轴/时间轴”“LRC/ELRC 源码”三种模式。
- 可从剪贴板替换，重新分行，移除空行；显示总行数和已打轴数，可显示/隐藏时间戳。
- 单行可编辑、插入、删除、拖拽排序、按当前播放时间打点、提前/延后 0.1 秒、清除时间戳。
- 可整体平移全部时间戳、按时间排序、清空全部。
- 与播放联动：播放/暂停、回退、拖动进度；若当前不是该歌曲，可直接播放本曲。
- 专注打轴模式突出上一句/当前句/下一句，提供上一行/下一行、打点、撤销/重做、微调和完成进度。
- 保存前若存在未打轴且会被丢弃的行，要提示继续编辑或仍然保存。

#### P20. 音乐源管理

支持的来源按类别组织：

- 本地：文件夹导入、多个散文件导入。
- NAS：Synology、QNAP；部分没有公开 API 的品牌显示“等待官方 API”，不可假装已支持。
- 通用协议：WebDAV、SMB/CIFS、FTP、SFTP、NFS、UPnP/DLNA、S3。
- 媒体服务器：Jellyfin、Emby、Plex。
- 音乐服务器：Subsonic、Navidrome、Airsonic、gonic、飞牛音乐、道理鱼。
- 云盘：百度网盘、阿里云盘、Google Drive、OneDrive、Dropbox、Drime、115、123 云盘。
- Apple Music。

来源列表：

- 按类别分组。每张来源卡显示图标、名称、类型、连接摘要、启用/停用、歌曲数、缓存占用和已选目录。
- 行内展示扫描状态、当前文件、总进度/已新增数量、失败详情、可恢复扫描、后台元数据补全、等待 Wi‑Fi、整源离线缓存进度。
- 根据来源能力提供：连接并选择目录/管理目录、扫描/继续扫描、整库深度扫描、缓存全部、导入本地文件、打开 Apple Music 设置。
- 侧滑或上下文菜单提供启用/停用、编辑、连接诊断、深度扫描、删除。
- 删除来源必须说明对资料库记录、歌单引用、离线文件和源端文件的影响，并提供可恢复路径或最近删除。

添加来源流程：

1. 首先选择来源类型；顶部可显示自动发现的局域网设备和重新扫描。
2. 本地导入分为文件夹和多文件，因为系统文件选择器能力不同；显示复制/导入进度并可取消。
3. 网络来源填写名称、主机、端口、路径、用户名/密码、SSL 和协议专属字段；OAuth 云盘走授权流程，Token/密钥类来源使用安全输入。
4. 连接过程有“连接中、密码失效重新输入、六位 OTP、SSL 信任、HTTP 风险、失败重试、成功浏览目录”等状态。
5. 目录浏览用面包屑逐层进入；勾选目录和进入目录是两个不同点击目标；底部显示已选数量和清除全部，完成后触发扫描。
6. 连接诊断逐项展示网络、认证、目录、读取等检查，可下拉刷新或重新运行。

#### P21. 歌单导入、重复歌曲与最近删除

歌单导入：

- 选择外部歌单文件并解析；大文件解析和本地曲库匹配不得阻塞 UI。
- 展示匹配成功数、缺失数和逐条匹配方式；允许为未匹配项手动搜索并指定本地歌曲。
- 输入新歌单名称后创建；可更换文件，并可导出未匹配项 CSV 便于后续处理。

重复歌曲：

- 扫描曲库后按重复组展示，显示重复组数、冗余文件数、可释放空间和每个版本的格式/质量/来源信息。
- 允许逐组选择保留项或按“保留最佳版本”等策略批量清理。
- 清理前二次确认；显示后台进度、结果、跳过/失败项；支持重新扫描。

最近删除：

- 分组显示已删除歌单、音乐源、刮削源配置，以及被隐藏的镜像歌单。
- 每项可恢复或永久删除；永久删除必须确认。
- 镜像歌单使用“恢复显示”，避免把隐藏和源端删除混为一谈。

#### P22. 设置总览

设置页按以下分组保留全部入口，并为每个详情页设计正常、禁用、未授权、加载、失败和成功状态：

1. **资料库**：音乐源、歌单导入、元数据刮削、歌词翻译、重复歌曲、存储管理、最近删除。
2. **播放**：播放设置、十段均衡器、音频效果、Siri。
3. **外观**：完整视觉风格、主题色、App 图标、首页布局、资料库显示方式；新增的全屏展示模式设置也放在这里或播放分组中，但必须单独成页。
4. **同步**：iCloud 同步、家庭共享。
5. **服务与集成**：Apple Music、Scrobble、聆听统计、DLNA Renderer。
6. **Apple TV**：推送资料库到电视、局域网 Relay。
7. **安全**：受信任域名、允许不安全 HTTP 的域名。
8. **关于**：版本、Build、检查更新、诊断报告、开源许可、App Store 评分、GitHub 仓库和反馈。

#### P23. 元数据、歌词翻译与存储

元数据刮削：

- 展示多个刮削源，支持启用/停用、拖拽优先级、编辑、删除和导入自定义刮削源。
- 支持“只补全缺失字段”，可恢复默认配置。
- 可发起“只刮削缺失元数据”或“重新刮削整个曲库”，开始前确认范围，进行中显示进度和取消。

歌词翻译：

- 总开关、目标语言、已缓存数量、清除缓存。
- Apple Translation 离线语言包需要下载时显示系统能力和状态。

存储管理：

- 云端扫描/元数据补全仅 Wi‑Fi 开关、完成通知及通知权限状态。
- 展示补全运行中、等待 Wi‑Fi、待处理、剩余数和失败重试。
- 音频缓存总开关和容量上限。
- 分类显示音频、封面、元数据占用；音频进一步区分固定离线、已完成、正在下载、部分文件、孤立文件。
- 可分别清理，危险操作确认；支持导出存储/扫描日志。

#### P24. 播放设置、EQ 与音频效果

播放设置：

- 输出模式：高保真最短链路 vs 启用音效处理。
- DSD：自动、PCM、DoP。
- 输出采样率匹配；高保真模式下按实际能力禁用不适用选项。
- 无缝播放与淡入淡出互斥；淡入淡出时长 1–12 秒。
- ReplayGain 开关及 Track/Album 模式。
- 空间音频及头部追踪。
- 播放速度 0.5–2.0，步进 0.05，可一键恢复 1.0。

十段 EQ：总开关、10 个垂直频段滑杆、内置/自定义预设卡、小型曲线预览、恢复默认。

音频效果：

- 混响：开关、预设、干湿比。
- 压缩器/限制器：开关、预设、阈值、Headroom、Attack、Release、Master Gain。
- 高保真模式绕过音效时，不要让控件看似可用；要说明为什么不可用以及如何切换输出模式。

#### P25. 同步、系统服务与外部设备

iCloud 同步：

- 总开关、账号/状态、上次同步、立即同步。
- 可分别控制歌单、来源、历史、设置、凭证、聆听统计；凭证同步有安全提示。
- 未登录 iCloud 时提供打开系统设置。

家庭共享：查看家庭状态、创建/管理、邀请成员、离开共享；破坏性操作单独放置并确认。

Apple Music：权限未决定/拒绝/已授权；授权后可同步/重新同步资料库，显示数量、日期、进度和错误。

Siri：显示权限，发起授权，拒绝后跳系统设置；说明可用语音能力。

Scrobble：

- 总开关和“发送正在播放”。
- ListenBrainz：Token 显示/隐藏、保存验证、连接状态、获取 Token 链接。
- Last.fm：内置网页 OAuth、等待回调、重新打开、确认/取消、用户名、退出；高级用户可填自定义 API Key/Secret。
- 展示待上报队列数、重试、清空，以及最近 5 条上报状态。

DLNA Renderer：启用、保持唤醒、运行状态、地址、已连接/正在投送设备、设备详情和最后出现时间。

Apple TV：将资料库快照/必要凭证推送到 TV，显示上传进度和成功/失败；Relay 提供局域网服务地址、启停和同步状态。

#### P26. 聆听统计与年度报告

聆听统计：

- 时间范围：周、月、年、全部。
- 摘要：播放次数、总时长、活跃天数、不同歌曲数。
- 可横向浏览的日历热力图和图例。
- 歌曲/艺术家/专辑排行榜，以播放次数和时长呈现。
- 支持清除全部统计，明确数据仅本地/同步策略并二次确认。

年度报告：

- 每年 1 月且上一年度数据跨度足够时自动出现一次，也应有手动入口。
- 全屏左右滑动故事卡，约 14 张：封面、总览、年度第一首、最爱艺术家、艺术家榜、歌曲榜、重要时刻、时段偏好、流派、探索、来源、峰值月份、听歌人格、结尾。
- 可关闭、分享当前卡或全部卡；分享输出必须与屏幕预览一致。

### 四、完整视觉风格切换系统

#### 1. 先区分三个概念

- **视觉风格 Visual Style**：决定整套界面的视觉语言和动效。
- **明暗模式 Appearance**：跟随系统/浅色/深色。
- **强调色 Accent Strategy**：跟随封面自动取色、使用风格默认色、使用固定预设色或用户自定义 HSB/HEX。

现有 App 已有“封面自动取色或固定强调色”的能力，但这不等于完整换肤。本次必须新增真正的 Visual Style 层，同时兼容现有强调色和 App 图标选择。

#### 2. 至少提供 5 套真正有差异的完整风格

请先提出每套风格的名称、设计叙事、适用人群、色彩、字体、形状、材质、图标、空间密度、动效和声音可视化策略，再落实到所有语义组件。建议至少包含：

1. **Ambient Glass / 封面氛围玻璃**：内容取色、柔和景深、克制玻璃材质、连续封面转场。
2. **Editorial Minimal / 黑白编辑感**：大字号排版、高留白、细线和网格、封面如唱片杂志，装饰最少。
3. **Neon Spectrum / 暗黑霓虹频谱**：深黑底、精确霓虹高亮、细致频谱和光晕；避免满屏廉价发光。
4. **Retro Hi‑Fi / 复古音响**：暖灰/奶油/木色、实体旋钮与 VU 表意象、唱片和器材质感，但操作仍符合现代触屏习惯。
5. **Liquid Aurora / 流体极光**：有层次的动态渐变、柔和流体形变、空间感和优雅粒子，优先用于播放和展示场景。

可以增加第 6 套“像素/磁带未来复古”或“自然静谧”，但不能用数量代替完成度。每套风格都必须覆盖全 App，不允许只有首页和播放页有效、进入设置后又变回系统默认样式。

#### 3. 建立语义化 Design Token，而不是在页面内硬编码颜色

每套风格至少定义并实现以下 Token：

- 背景：base、grouped、surface、elevated、overlay、scrim。
- 文本：primary、secondary、tertiary、onAccent、disabled。
- 品牌与语义色：accent、positive、warning、danger、info、live、offline、selection。
- 分隔、描边、阴影、材质、模糊强度、封面取色混合规则。
- 字体：大标题、页面标题、区块标题、正文、辅助文字、数值、等宽技术信息、歌词当前/过去/未来。
- 圆角、封面蒙版、边框粗细、卡片层级、阴影和海拔。
- 4/8pt 间距体系、页面边距、列表密度、网格列宽和触控尺寸。
- 图标尺寸、粗细、填充/线框策略和选中态；不改变图标原有语义。
- 动效：页面转场、封面切歌、按压、选择、展开、加载、主题切换的 duration/easing/spring。
- 触觉：选择、确认、危险操作、播放切换的统一等级。
- 图表、热力图、频谱、歌词高亮、进度条和缓冲条。

将按钮、图标按钮、卡片、歌曲行、专辑卡、艺术家卡、歌单行、Section Header、Tab Bar、侧边栏、迷你播放器、搜索框、表单、Toggle、Picker、Slider、菜单、Sheet、Alert、Toast、Banner、Skeleton、Empty/Error/Offline View、批量选择栏全部做成消费 Token 的共享语义组件。

#### 4. 主题切换交互

- 在“设置 > 外观 > 视觉风格”提供大尺寸预览卡；预览卡至少模拟首页卡片、歌曲行、播放控件和歌词，而不只是显示色块。
- 点击先进入全屏/半屏实时预览，可在示例内容和当前真实页面之间切换；用户确认后应用。
- 应用时进行 250–450ms 的柔和跨主题过渡；减弱动态效果时直接切换或简单淡变。
- 切换过程不得停止音乐、重建队列、关闭当前 Sheet、重置导航、清空搜索、滚回列表顶部或重新请求网络。
- 风格、明暗和强调色分别持久化；明确哪些只保存在本机、哪些参与 iCloud 同步。
- 提供“跟随系统时间自动切换明暗”，但不要未经用户允许按时间自动更换整套风格。
- 允许每套风格保存自己的强调色策略和动效强度；切回该风格时恢复。
- 可选“跟随当前封面取色”，但必须经过对比度算法，不能出现文字不可读或危险按钮被染成普通色。

#### 5. 完整覆盖和性能要求

- 所有页面、Sheet、弹窗、上下文菜单、系统权限说明、错误提示、Widget 关联色和外接屏展示都需要一致策略。
- 系统原生控件无法完全换肤时，选择符合平台习惯的安全方案，不要用脆弱私有 API 模拟。
- 长列表不要逐行做实时高斯模糊、Canvas 粒子或封面取色；封面颜色应缓存。
- 静止页面目标 60fps；ProMotion 设备的关键播放动画可适配 120Hz；主题切换不能触发全库重算。
- 保持至少 44×44pt 触控区；支持大字体和加粗文字；颜色对比满足 WCAG AA，选中/错误不能只靠颜色。

### 五、独立新增需求：全屏展示模式（Showcase / Ambient Display）

这是独立功能，不等同于普通“正在播放”页，也不等同于完整视觉风格。任意 App 风格都可以选择任意展示效果，二者应解耦。

#### 1. 目标场景

- 手机横放在桌面、支架、音响旁或充电座上，作为长期展示型音乐屏。
- 画面全屏、无干扰、足够漂亮，适合演示、拍摄和空间氛围。
- 可在播放歌曲、Apple Music、网络电台、暂停和无歌曲时分别给出合适画面。
- 当前工程已有外接屏“大封面 + 封面取色氛围背景 + 大字歌词”和音频频谱基础，优先复用其数据与渲染能力，不要另建相互冲突的播放状态。

#### 2. 入口与退出

- 正在播放页提供明显但不喧宾夺主的“进入展示模式”按钮。
- 设置中有独立“全屏展示”页，可选择默认效果、自动息屏策略、叠加信息和性能等级。
- 可选：长按封面或迷你播放器快捷进入，但不能与现有长按/滑动手势冲突。
- 进入时全屏覆盖、隐藏状态栏和普通导航；支持横竖屏，横屏应重新排版而不是旋转放大。
- 单击显示/隐藏控制层；下滑或明确关闭按钮退出；锁定后必须先解锁才能退出或误触操作。

#### 3. 至少提供 7 种可切换展示场景

每种效果都应有独立构图、动效和降级版本，而不是仅换背景颜色：

1. **Album Stage**：巨幅封面、缓慢景深/视差、封面色氛围光、精致曲目信息。
2. **Lyric Cinema**：超大同步歌词，当前句/逐字高亮，前后句形成空间层次；支持纯歌词和封面 + 歌词双栏。
3. **Spectrum Arena**：频谱柱、镜像波形或环形频谱，真正响应音频强度，配色可跟随封面。
4. **Vinyl Deck**：旋转黑胶/唱片封套、唱臂或唱机意象；暂停时自然减速而非突然停止。
5. **Liquid Aurora**：音频驱动的流体渐变/极光，低频影响形变、中高频影响细节与亮度。
6. **Particle Nebula**：克制的音频粒子、星云或光点轨迹，不能遮挡文本或造成视觉噪声。
7. **Minimal Clock**：时钟、日期、歌曲名、封面小组件和极简进度，适合长时间摆放和无歌曲状态。

可增加 **VU Hi‑Fi**（双 VU 表/电平仪）、**Karaoke Stage**（更强逐字演唱效果）、**External Display**（左封面右大字歌词）和自动轮播场景。

#### 4. 展示模式交互

- 在空白区域左右滑切换效果；切换时使用连续转场并显示效果名称短暂 Toast。
- 单击唤出半透明控制层：上一首、播放/暂停、下一首、进度、喜欢、队列、AirPlay/DLNA、退出、锁定。
- 长按打开“场景选择器”，以可实时动起来的缩略预览选择。
- 设置面板可调：效果强度、音频灵敏度、速度、粒子数量、模糊、封面/品牌/自定义调色、歌词/时钟/专辑/音质信息开关、控制层自动隐藏时间。
- 可收藏场景预设，并设置每 1/3/5/10 分钟自动轮播；切歌时可选保持当前场景或按曲目自动切换。
- 控制层默认 3–5 秒自动隐藏；隐藏后不得留下静态按钮残影。
- 网络电台使用 LIVE 构图；没有总时长时隐藏普通进度。暂停或缓冲时，动效平滑减弱并显示状态。

#### 5. 防息屏、OLED 与能耗策略

- 提供“展示模式期间保持屏幕常亮”开关，默认先解释耗电和发热；可选“始终”“仅连接电源时”“仍按系统设置息屏”。
- 仅在展示模式处于前台且用户已授权时禁用系统自动锁定；退出、进入后台、场景断开或发生异常时必须立即恢复原系统行为。
- 不承诺 iOS 后台仍保持屏幕常亮；明确这是前台展示能力。
- 提供 OLED 防烧屏：静态文字/封面做极慢小范围漂移，定时改变构图，长时间无操作后降低亮度感和静态元素不透明度，避免持续满白和固定高亮。
- 低电量模式、严重发热、非充电状态下自动降低帧率/粒子/模糊，并以非打扰提示说明；允许用户选择性能等级“节能/平衡/极致”。
- App 进入后台时暂停 GPU/频谱渲染；回到前台平滑恢复。
- 不擅自永久修改系统亮度；若提供临时亮度控制，必须获得明确操作并在退出时恢复原值。

#### 6. 音频数据不可用时的降级

- 高保真最短链路会绕过 EQ、音效和可视化 Tap；Apple Music 也可能不提供 PCM；部分网络电台只提供有限元数据。
- 因此每个依赖 FFT/波形的场景必须有优雅降级：使用播放进度、封面色、缓慢程序化呼吸动画或静态精美构图，不能伪装成真实频谱。
- 在用户选择依赖实时频谱的效果而当前模式不可用时，以简短说明告知“当前输出模式不提供实时音频数据”，并允许切换到兼容效果；绝不能为了动画偷偷切换高保真输出模式。

### 六、统一交互与状态规范

为每个关键页面和共享组件给出以下状态，而不是只画理想数据充足态：

- 初始加载、局部刷新、分页/增量加载。
- 空资料库、空分类、无搜索结果、无歌词、无封面、无网络。
- 扫描中、可恢复扫描、元数据后台补全、等待 Wi‑Fi。
- 缓冲、播放失败、来源离线、凭证失效、权限拒绝、SSL/HTTP 风险。
- 离线已缓存、下载中、部分缓存、缓存失败、重试。
- 单选、多选、全选、批量进度、部分成功、跳过和撤销。
- 可编辑、只读、系统保护、Apple Music 能力受限。
- 删除确认、永久删除、源文件删除和从资料库移除的语义差异。

交互规则：

- 主操作单击完成；长按用于上下文或进入多选；侧滑只承载高频且可预期的行操作。
- 所有图标按钮有无障碍标签；危险按钮不能只用垃圾桶图标而无文字上下文。
- 触觉只用于关键状态变化，滚动和每帧动画不要滥用。
- Toast 用于轻量结果，Banner 用于持续状态，Alert 用于必须做决定的风险，Sheet 用于可中断子任务，全屏用于沉浸任务。
- 键盘出现时保存/搜索/下一步按钮必须可达；Sheet 支持合适的 medium/large detent 和交互式关闭保护。

### 七、你必须输出的成果

不要直接给几张漂亮图就结束。按以下顺序交付：

1. **现有功能核对表**：逐页确认上文 P00–P26，无遗漏，并标出任何从源码发现的差异。
2. **新版信息架构与核心流程**：首页听歌、添加来源、扫描入库、搜索歌词命中、播放与队列、批量离线、智能歌单、主题切换、进入/退出展示模式。
3. **页面清单与状态矩阵**：每个页面的入口、主要信息、主/次操作、手势、Sheet/Alert、空/错/加载/权限状态。
4. **完整设计系统**：基础 Token、语义 Token、共享组件、响应式断点、可访问性规则。
5. **至少 5 套完整视觉风格说明**：每套给出 Moodboard 关键词、颜色、字体、形状、材质、图标、动效、页面应用示例和适配明暗策略。
6. **关键页面高保真设计矩阵**：每套风格至少完整展示首页、资料库、歌曲列表、搜索、标准播放页、歌词页、设置/表单、来源管理和一个空/错状态；其余页面通过已定义组件和 Token 保证可推导，不得出现未覆盖的系统默认孤岛。
7. **全屏展示模式专案**：场景选择器、至少 7 种场景、横竖屏构图、控制层、锁定、防息屏、OLED/能耗和数据降级。
8. **可点击原型与动效规格**：标注手势、转场时长、Easing、自动隐藏、拖拽阈值、主题切换和切歌动画。
9. **若可写代码，给出并实施架构方案**：例如 `AppVisualStyle`、`AppearanceMode`、`AccentStrategy`、`ThemeTokens`/Environment、语义组件层、持久化与迁移、`ShowcaseScene` 协议、音频可视化数据源和降级策略。命名可按工程现状调整，不要为了套用示例而破坏现有架构。
10. **验收清单和测试计划**：视觉覆盖、功能回归、主题热切换、播放不中断、长列表性能、横竖屏、大字体、VoiceOver、减弱动态效果、低电量/发热、Apple Music/电台/高保真降级。

### 八、明确禁止的结果

- 禁止只改品牌色、深浅色或封面背景，却称为 5 套完整主题。
- 禁止只设计首页和播放页，设置、表单、来源管理继续使用毫无关联的默认样式。
- 禁止为了画面简洁删除批量操作、来源管理、专业播放信息、智能歌单或错误状态。
- 禁止把所有页面都做成同一种大圆角玻璃卡片，失去层级和内容密度。
- 禁止用装饰动画阻挡歌词、控件或降低长列表性能。
- 禁止主题切换时重启 App、停止播放、丢失队列或重置导航。
- 禁止把模拟呼吸动画宣传为真实频谱；数据不可用时必须诚实降级。
- 禁止在未经明确选择时强制常亮、强制最高亮度或绕过高保真模式。
- 禁止输出无法实现的“概念魔法”；所有方案都要说明 iOS/SwiftUI 下的落地方式、限制和替代方案。

最终目标是：用户可在同一个成熟音乐 App 中一键切换多套真正完整、个性鲜明且功能等价的视觉风格；同时可把手机放在桌面上进入长期、稳定、漂亮、可交互的全屏音乐展示模式。设计必须既有令人想分享的视觉冲击力，也能承载 Primuse 复杂且专业的资料库与播放能力。

## Prompt 正文结束
